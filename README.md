# Wagtopia

A dog grooming appointment management app built with Flask and SQLite.

## Install Dependencies

```bash
pip install -r requirements.txt
```

## Set Up the Database

```bash
sqlite3 wagtopia.db < schema.sql
python seed.py
```

## Start the Application

```bash
python app.py
```

## Access the Application

Check the terminal output for the URL.